<%= form_for(@admin_name_exercise) do |f| %>
  <% if @admin_name_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_name_exercise.errors.count, "error") %> prohibited this admin_name_exercise from being saved:</h2>

      <ul>
      <% @admin_name_exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
