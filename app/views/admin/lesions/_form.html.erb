<div class="form-style" >
    <%= form_for(@admin_lesion) do |f| %>
      <% if @admin_lesion.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@admin_lesion.errors.count, "error") %> prohibited this admin_lesion from being saved:</h2>

          <ul>
          <% @admin_lesion.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :name_lesion %><br>
        <%= f.text_field :name_lesion %>
      </div>
      <div class="field">
        <%= f.label :description %><br>
        <%= f.text_area :description %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
</div>